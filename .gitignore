/target
# `crisol package` writes here by default.
/dist
**/*.rs.bk
*.pdb

# Render snapshot failures are written next to the expected image.
tests/render-snapshots/**/*.actual.png
tests/layout-snapshots/**/*.actual.json
tests/ir-snapshots/**/*.actual

.DS_Store
