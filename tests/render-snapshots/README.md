# render-snapshots

Documents paired with the pixels they must produce. Lands at M4, once there is text to render; until then the renderer's own offscreen tests in `renderer/wgpu/tests/render.rs` cover the pixel path.
